# ===================================
# CẤU HÌNH BACKEND - TASK MANAGER APP
# ===================================

# --- PORT ---
PORT=5000

# --- MONGODB ---
# Cách 1: MongoDB Local
MONGO_URI=mongodb://localhost:27017/task_manager_db

# Cách 2: MongoDB Atlas (Comment dòng trên, uncomment dòng dưới)
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/task_manager_db?retryWrites=true&w=majority

# --- JWT SECRET ---
# Đổi thành chuỗi ngẫu nhiên phức tạp của bạn
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_123456

# --- EMAIL SERVICE (Optional - cho tính năng gửi thông báo) ---
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here

# Lưu ý: 
# - Với Gmail, cần tạo "App Password" trong Google Account
# - Không dùng mật khẩu thật của email
# - Vào: https://myaccount.google.com/apppasswords

# --- NODE ENVIRONMENT ---
NODE_ENV=development
# Đổi thành 'production' khi deploy

# ===================================
# HƯỚNG DẪN NHANH:
# ===================================
# 1. Copy file này thành '.env' (bỏ .example)
# 2. Thay đổi các giá trị phù hợp
# 3. Với MongoDB Local: Đảm bảo MongoDB đã chạy (mongod)
# 4. Với MongoDB Atlas: Thay username, password, và cluster URL
# 5. JWT_SECRET: Đổi thành chuỗi bí mật của bạn
# 6. EMAIL: Chỉ cần nếu muốn gửi email thông báo
